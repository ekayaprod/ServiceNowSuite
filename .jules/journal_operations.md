**Vulnerability:** Loose falsy checks in DOM manipulation methods (`showStep` and `showResult` in `ticket_template.html`) allowed invalid inputs (like `null`, `undefined`, or empty strings) to execute downstream UI logic, potentially causing structural degradation or runtime crashes. | **Prevention:** Ban loose falsy evaluation. Enforce strict type validation (`=== null`, `=== undefined`) and early returns at method entry points, annotated with explicit `[SECURITY BOUNDARY]` inline comments to safely discard invalid boundaries.

**Learning:** Bookmarklet minification logic in the browser involves replacing template literals stringified code that is heavily dependent on correctly escaping backticks. We must map `\`` directly into the raw source instead of over-escaping them when we do literal regex replacements. | **Action:** Always double check the browser AST compilation rules when directly injecting stringified JS bookmarklet variables and avoid using Python's `\\` if replacing directly in JavaScript contexts.
**Learning:** Empty catch blocks in browser bookmarklet generators hide cross-origin frame access failures and click handling bugs, making debugging impossible when a script fails to boot in a host environment like ServiceNow. | **Action:** Always upgrade empty `catch(e){}` blocks to at least include a `console.warn` or `console.error` with the specific execution context and `e.message` to preserve telemetry.
